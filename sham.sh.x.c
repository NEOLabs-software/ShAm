#if 0
	shc Version 4.0.3, Generic Shell Script Compiler
	GNU GPL Version 3 Md Jahidul Hamid <jahidulhamid@yahoo.com>

	shc -f sham.sh 
#endif

static  char data [] = 
#define      msg2_z	19
#define      msg2	((&data[2]))
	"\163\125\056\101\362\371\346\240\205\221\106\036\366\100\130\301"
	"\111\357\313\057\147\211\166"
#define      rlax_z	1
#define      rlax	((&data[23]))
	"\104"
#define      chk2_z	19
#define      chk2	((&data[24]))
	"\037\255\314\141\135\347\123\015\046\260\313\354\223\312\212\203"
	"\243\175\060\060\254\347"
#define      lsto_z	1
#define      lsto	((&data[46]))
	"\362"
#define      opts_z	1
#define      opts	((&data[47]))
	"\276"
#define      text_z	672
#define      text	((&data[204]))
	"\021\065\164\340\156\064\173\354\246\357\101\057\145\367\221\054"
	"\353\303\330\033\160\277\221\166\276\121\351\140\006\326\332\027"
	"\014\117\370\173\203\164\147\052\143\251\131\311\241\353\365\214"
	"\256\315\247\037\215\071\225\114\212\177\254\221\126\207\251\142"
	"\327\241\335\133\026\105\205\171\356\337\102\217\312\067\034\171"
	"\005\303\230\223\375\056\337\210\256\214\031\004\024\303\147\353"
	"\144\104\106\172\212\314\364\171\253\066\010\165\156\044\357\164"
	"\350\210\007\346\266\347\156\145\164\210\151\210\113\320\164\260"
	"\025\273\053\237\207\037\031\062\126\041\250\304\106\227\071\057"
	"\037\101\025\326\050\204\073\234\015\245\045\131\105\307\346\101"
	"\063\315\006\007\044\117\370\340\322\015\164\022\336\107\267\332"
	"\115\133\165\120\104\325\357\326\061\214\157\137\214\176\343\104"
	"\047\123\057\241\007\312\202\262\232\274\021\152\175\374\251\007"
	"\205\231\226\242\143\234\263\362\221\172\223\172\073\251\043\213"
	"\011\036\205\056\173\030\300\054\143\056\334\375\314\216\112\241"
	"\334\337\262\121\071\045\147\210\266\136\261\014\070\020\166\071"
	"\254\330\140\110\127\377\033\331\361\062\045\001\322\144\112\060"
	"\275\030\001\337\177\071\055\271\253\307\257\272\064\001\220\341"
	"\152\062\025\247\145\206\232\241\076\131\300\242\045\024\216\043"
	"\362\353\202\063\115\147\372\275\024\117\111\233\206\100\173\224"
	"\310\161\063\160\365\077\141\141\306\130\042\061\157\037\300\245"
	"\033\357\215\341\247\061\255\223\147\026\365\374\265\216\251\146"
	"\124\240\131\225\015\222\104\014\017\306\001\065\211\141\304\041"
	"\132\361\316\356\071\140\030\001\277\066\114\235\001\375\030\331"
	"\035\026\146\037\161\252\053\020\152\341\302\171\041\353\335\201"
	"\235\064\151\210\067\046\305\312\213\156\272\064\010\377\340\324"
	"\350\140\037\106\262\252\136\173\153\337\237\341\275\313\267\003"
	"\240\211\260\355\143\102\110\164\035\176\200\036\342\075\063\233"
	"\035\353\240\175\116\231\307\137\325\372\017\256\010\316\174\043"
	"\011\126\332\174\043\224\371\031\111\220\324\135\341\136\256\013"
	"\240\023\133\041\244\025\043\315\234\114\366\001\207\217\025\046"
	"\346\314\352\251\162\304\223\305\020\157\262\307\246\042\206\235"
	"\276\340\155\120\270\365\335\323\275\341\055\145\151\150\143\320"
	"\132\157\243\057\247\071\046\311\174\010\300\130\046\157\275\145"
	"\361\260\312\267\155\044\345\022\266\374\307\107\201\104\025\304"
	"\071\317\305\013\042\305\257\031\264\101\133\354\017\276\313\335"
	"\163\342\171\177\176\343\041\261\161\304\037\063\006\020\045\035"
	"\305\312\156\370\304\326\252\046\230\102\227\214\077\344\100\373"
	"\364\064\156\025\262\176\253\245\006\210\365\344\066\064\327\131"
	"\232\334\067\243\260\254\330\060\076\172\064\275\100\101\002\320"
	"\013\363\001\123\001\257\027\252\114\054\345\055\113\005\116\265"
	"\162\047\351\347\377\261\304\221\167\044\324\222\130\262\314\162"
	"\313\066\250\002\174\132\127\071\272\211\175\377\006\205\035\300"
	"\151\254\220\176\052\230\205\335\310\264\043\163\137\203\363\104"
	"\210\343\364\346\267\034\341\237\163\032\032\154\066\216\357\130"
	"\141\222\004\262\051\214\016\213\361\274\230\011\301\047\256\114"
	"\336\203\161\257\017\055\040\267\222\021\046\005\376\307\226\134"
	"\335\322\001\155\305\012\364\017\032\116\330\076\163\232\026\075"
	"\316\054\120\271\301\273\041\225\017\330\237\206\022\123\155\374"
	"\347\217\037\241\072\177\370\203\233\322\162\277\353\261\201\212"
	"\262\060\075\144\205\106\257\366\003\030\027\144\041\221\120\113"
	"\157\124\260\070\235\154\127\245\125\323\057\352\166\231\012\213"
	"\124\065\053\333\125\104\016\253\146\266\160\255\115\251\334\155"
	"\352\362\103\023\167\177\260\205\045\325\336\233\157\350\047\303"
	"\036\123\237\163\230\255\037\377\143\217\254\261\071\211\036\043"
	"\174\142\066\363\342\347\171\007\274\130\243\053\100\312\357\137"
	"\036\216\323\266\074\362\266\237\201\142\120\272"
#define      tst2_z	19
#define      tst2	((&data[958]))
	"\150\322\246\174\245\150\260\003\133\047\045\201\004\124\106\253"
	"\306\066\172\335\053\025\134\264\374"
#define      pswd_z	256
#define      pswd	((&data[1028]))
	"\056\136\344\156\051\323\316\107\142\241\376\236\223\264\076\025"
	"\027\217\317\003\376\256\154\320\303\311\204\277\237\100\170\315"
	"\237\135\074\311\061\012\021\223\254\020\062\077\304\160\124\051"
	"\313\046\277\270\234\312\233\324\151\375\315\226\335\124\162\172"
	"\364\340\066\003\144\166\257\362\064\045\222\160\127\105\231\043"
	"\153\131\334\007\043\167\334\215\165\252\043\123\377\226\315\363"
	"\167\004\366\333\173\245\315\260\312\140\040\042\245\271\105\021"
	"\023\042\031\066\231\366\304\016\240\350\141\237\176\057\223\365"
	"\064\211\320\257\057\235\140\371\376\200\034\244\072\142\265\115"
	"\204\317\204\036\305\110\054\145\060\216\005\256\276\231\244\363"
	"\042\164\242\121\022\002\113\021\203\150\265\275\312\152\013\117"
	"\071\217\155\377\330\231\144\011\050\152\270\346\003\134\331\046"
	"\321\174\170\343\177\303\364\003\054\252\301\366\025\314\106\117"
	"\134\263\116\064\115\263\076\165\036\366\134\042\122\066\110\044"
	"\263\300\007\063\204\374\066\261\246\367\250\273\303\356\013\040"
	"\241\131\124\356\014\222\144\052\211\301\114\334\367\225\000\252"
	"\126\007\336\333\004\024\214\253\013\064\147\317\042\162\357\304"
	"\313\104\262\330\327\027\003\140\330\117\075\320\345\075\172\073"
	"\105\131\026\111\155\243\365\171\330\134\111\372\317\070\277\334"
	"\377\044\340\376\322\115\316\225\026\123\125\266\224\316\203\064"
	"\053\277\375\134\312\016\357\166\036\041\266\343\222\012\300\221"
	"\057\240\217\001\355\136\227\004\262\354\272\107\272"
#define      shll_z	10
#define      shll	((&data[1331]))
	"\375\107\244\206\031\231\361\222\276\271\006\171\102"
#define      msg1_z	65
#define      msg1	((&data[1352]))
	"\077\246\124\365\212\346\000\112\167\374\273\355\106\050\241\152"
	"\253\326\121\076\376\203\356\104\070\106\336\300\023\075\117\263"
	"\031\020\144\260\320\340\025\325\174\317\156\253\046\146\074\255"
	"\070\163\142\202\070\261\342\137\220\311\277\301\023\171\264\212"
	"\331\307\134\276\002\124\225\345\075\010\057\353\007"
#define      date_z	1
#define      date	((&data[1420]))
	"\147"
#define      inlo_z	3
#define      inlo	((&data[1421]))
	"\303\127\100"
#define      xecc_z	15
#define      xecc	((&data[1426]))
	"\160\324\374\307\166\124\162\130\026\254\171\106\275\037\104\032"
	"\265\333"
#define      tst1_z	22
#define      tst1	((&data[1446]))
	"\231\233\334\313\041\034\153\115\124\300\061\173\256\370\107\272"
	"\217\060\156\145\345\166\325\001\320\170\332"
#define      chk1_z	22
#define      chk1	((&data[1469]))
	"\125\032\355\045\263\346\343\134\377\261\251\133\350\362\275\142"
	"\074\252\135\141\307\051\015\005\320\130"/* End of data[] */;
#define      hide_z	4096
#define SETUID 0	/* Define as 1 to call setuid(0) at start of script */
#define DEBUGEXEC	0	/* Define as 1 to debug execvp calls */
#define TRACEABLE	1	/* Define as 1 to enable ptrace the executable */
#define HARDENING	0	/* Define as 1 to disable ptrace/dump the executable */
#define BUSYBOXON	0	/* Define as 1 to enable work with busybox */

#if HARDENING
static const char * shc_x[] = {
"/*",
" * Copyright 2019 - Intika <intika@librefox.org>",
" * Replace ******** with secret read from fd 21",
" * Also change arguments location of sub commands (sh script commands)",
" * gcc -Wall -fpic -shared -o shc_secret.so shc_secret.c -ldl",
" */",
"",
"#define _GNU_SOURCE /* needed to get RTLD_NEXT defined in dlfcn.h */",
"#define PLACEHOLDER \"********\"",
"#include <dlfcn.h>",
"#include <stdlib.h>",
"#include <string.h>",
"#include <unistd.h>",
"#include <stdio.h>",
"#include <signal.h>",
"",
"static char secret[128000]; //max size",
"typedef int (*pfi)(int, char **, char **);",
"static pfi real_main;",
"",
"// copy argv to new location",
"char **copyargs(int argc, char** argv){",
"    char **newargv = malloc((argc+1)*sizeof(*argv));",
"    char *from,*to;",
"    int i,len;",
"",
"    for(i = 0; i<argc; i++){",
"        from = argv[i];",
"        len = strlen(from)+1;",
"        to = malloc(len);",
"        memcpy(to,from,len);",
"        // zap old argv space",
"        memset(from,'\\0',len);",
"        newargv[i] = to;",
"        argv[i] = 0;",
"    }",
"    newargv[argc] = 0;",
"    return newargv;",
"}",
"",
"static int mymain(int argc, char** argv, char** env) {",
"    //fprintf(stderr, \"Inject main argc = %d\\n\", argc);",
"    return real_main(argc, copyargs(argc,argv), env);",
"}",
"",
"int __libc_start_main(int (*main) (int, char**, char**),",
"                      int argc,",
"                      char **argv,",
"                      void (*init) (void),",
"                      void (*fini)(void),",
"                      void (*rtld_fini)(void),",
"                      void (*stack_end)){",
"    static int (*real___libc_start_main)() = NULL;",
"    int n;",
"",
"    if (!real___libc_start_main) {",
"        real___libc_start_main = dlsym(RTLD_NEXT, \"__libc_start_main\");",
"        if (!real___libc_start_main) abort();",
"    }",
"",
"    n = read(21, secret, sizeof(secret));",
"    if (n > 0) {",
"      int i;",
"",
"    if (secret[n - 1] == '\\n') secret[--n] = '\\0';",
"    for (i = 1; i < argc; i++)",
"        if (strcmp(argv[i], PLACEHOLDER) == 0)",
"          argv[i] = secret;",
"    }",
"",
"    real_main = main;",
"",
"    return real___libc_start_main(mymain, argc, argv, init, fini, rtld_fini, stack_end);",
"}",
"",
0};
#endif /* HARDENING */

/* rtc.c */

#include <sys/stat.h>
#include <sys/types.h>

#include <errno.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <time.h>
#include <unistd.h>

/* 'Alleged RC4' */

static unsigned char stte[256], indx, jndx, kndx;

/*
 * Reset arc4 stte. 
 */
void stte_0(void)
{
	indx = jndx = kndx = 0;
	do {
		stte[indx] = indx;
	} while (++indx);
}

/*
 * Set key. Can be used more than once. 
 */
void key(void * str, int len)
{
	unsigned char tmp, * ptr = (unsigned char *)str;
	while (len > 0) {
		do {
			tmp = stte[indx];
			kndx += tmp;
			kndx += ptr[(int)indx % len];
			stte[indx] = stte[kndx];
			stte[kndx] = tmp;
		} while (++indx);
		ptr += 256;
		len -= 256;
	}
}

/*
 * Crypt data. 
 */
void arc4(void * str, int len)
{
	unsigned char tmp, * ptr = (unsigned char *)str;
	while (len > 0) {
		indx++;
		tmp = stte[indx];
		jndx += tmp;
		stte[indx] = stte[jndx];
		stte[jndx] = tmp;
		tmp += stte[indx];
		*ptr ^= stte[tmp];
		ptr++;
		len--;
	}
}

/* End of ARC4 */

#if HARDENING

#include <sys/ptrace.h>
#include <sys/wait.h>
#include <signal.h>
#include <sys/prctl.h>
#define PR_SET_PTRACER 0x59616d61

/* Seccomp Sandboxing Init */
#include <stdlib.h>
#include <stdio.h>
#include <stddef.h>
#include <string.h>
#include <unistd.h>
#include <errno.h>

#include <sys/types.h>
#include <sys/prctl.h>
#include <sys/syscall.h>
#include <sys/socket.h>

#include <linux/filter.h>
#include <linux/seccomp.h>
#include <linux/audit.h>

#define ArchField offsetof(struct seccomp_data, arch)

#define Allow(syscall) \
    BPF_JUMP(BPF_JMP+BPF_JEQ+BPF_K, SYS_##syscall, 0, 1), \
    BPF_STMT(BPF_RET+BPF_K, SECCOMP_RET_ALLOW)

struct sock_filter filter[] = {
    /* validate arch */
    BPF_STMT(BPF_LD+BPF_W+BPF_ABS, ArchField),
    BPF_JUMP( BPF_JMP+BPF_JEQ+BPF_K, AUDIT_ARCH_X86_64, 1, 0),
    BPF_STMT(BPF_RET+BPF_K, SECCOMP_RET_KILL),

    /* load syscall */
    BPF_STMT(BPF_LD+BPF_W+BPF_ABS, offsetof(struct seccomp_data, nr)),

    /* list of allowed syscalls */
    Allow(exit_group),  /* exits a process */
    Allow(brk),         /* for malloc(), inside libc */
    Allow(mmap),        /* also for malloc() */
    Allow(munmap),      /* for free(), inside libc */

    /* and if we don't match above, die */
    BPF_STMT(BPF_RET+BPF_K, SECCOMP_RET_KILL),
};
struct sock_fprog filterprog = {
    .len = sizeof(filter)/sizeof(filter[0]),
    .filter = filter
};

/* Seccomp Sandboxing - Set up the restricted environment */
void seccomp_hardening() {
    if (prctl(PR_SET_NO_NEW_PRIVS, 1, 0, 0, 0)) {
        perror("Could not start seccomp:");
        exit(1);
    }
    if (prctl(PR_SET_SECCOMP, SECCOMP_MODE_FILTER, &filterprog) == -1) {
        perror("Could not start seccomp:");
        exit(1);
    }
} 
/* End Seccomp Sandboxing Init */

void shc_x_file() {
    FILE *fp;
    int line = 0;

    if ((fp = fopen("/tmp/shc_x.c", "w")) == NULL ) {exit(1); exit(1);}
    for (line = 0; shc_x[line]; line++)	fprintf(fp, "%s\n", shc_x[line]);
    fflush(fp);fclose(fp);
}

int make() {
	char * cc, * cflags, * ldflags;
    char cmd[4096];

	cc = getenv("CC");
	if (!cc) cc = "cc";

	sprintf(cmd, "%s %s -o %s %s", cc, "-Wall -fpic -shared", "/tmp/shc_x.so", "/tmp/shc_x.c -ldl");
	if (system(cmd)) {remove("/tmp/shc_x.c"); return -1;}
	remove("/tmp/shc_x.c"); return 0;
}

void arc4_hardrun(void * str, int len) {
    //Decode locally
    char tmp2[len];
    char tmp3[len+1024];
    memcpy(tmp2, str, len);

	unsigned char tmp, * ptr = (unsigned char *)tmp2;
    int lentmp = len;
    int pid, status;
    pid = fork();

    shc_x_file();
    if (make()) {exit(1);}

    setenv("LD_PRELOAD","/tmp/shc_x.so",1);

    if(pid==0) {

        //Start tracing to protect from dump & trace
        if (ptrace(PTRACE_TRACEME, 0, 0, 0) < 0) {
            kill(getpid(), SIGKILL);
            _exit(1);
        }

        //Decode Bash
        while (len > 0) {
            indx++;
            tmp = stte[indx];
            jndx += tmp;
            stte[indx] = stte[jndx];
            stte[jndx] = tmp;
            tmp += stte[indx];
            *ptr ^= stte[tmp];
            ptr++;
            len--;
        }

        //Do the magic
        sprintf(tmp3, "%s %s", "'********' 21<<<", tmp2);

        //Exec bash script //fork execl with 'sh -c'
        system(tmp2);

        //Empty script variable
        memcpy(tmp2, str, lentmp);

        //Clean temp
        remove("/tmp/shc_x.so");

        //Sinal to detach ptrace
        ptrace(PTRACE_DETACH, 0, 0, 0);
        exit(0);
    }
    else {wait(&status);}

    /* Seccomp Sandboxing - Start */
    seccomp_hardening();

    exit(0);
}
#endif /* HARDENING */

/*
 * Key with file invariants. 
 */
int key_with_file(char * file)
{
	struct stat statf[1];
	struct stat control[1];

	if (stat(file, statf) < 0)
		return -1;

	/* Turn on stable fields */
	memset(control, 0, sizeof(control));
	control->st_ino = statf->st_ino;
	control->st_dev = statf->st_dev;
	control->st_rdev = statf->st_rdev;
	control->st_uid = statf->st_uid;
	control->st_gid = statf->st_gid;
	control->st_size = statf->st_size;
	control->st_mtime = statf->st_mtime;
	control->st_ctime = statf->st_ctime;
	key(control, sizeof(control));
	return 0;
}

#if DEBUGEXEC
void debugexec(char * sh11, int argc, char ** argv)
{
	int i;
	fprintf(stderr, "shll=%s\n", sh11 ? sh11 : "<null>");
	fprintf(stderr, "argc=%d\n", argc);
	if (!argv) {
		fprintf(stderr, "argv=<null>\n");
	} else { 
		for (i = 0; i <= argc ; i++)
			fprintf(stderr, "argv[%d]=%.60s\n", i, argv[i] ? argv[i] : "<null>");
	}
}
#endif /* DEBUGEXEC */

void rmarg(char ** argv, char * arg)
{
	for (; argv && *argv && *argv != arg; argv++);
	for (; argv && *argv; argv++)
		*argv = argv[1];
}

void chkenv_end(void);

int chkenv(int argc)
{
	char buff[512];
	unsigned long mask, m;
	int l, a, c;
	char * string;
	extern char ** environ;

	mask = (unsigned long)getpid();
	stte_0();
	 key(&chkenv, (void*)&chkenv_end - (void*)&chkenv);
	 key(&data, sizeof(data));
	 key(&mask, sizeof(mask));
	arc4(&mask, sizeof(mask));
	sprintf(buff, "x%lx", mask);
	string = getenv(buff);
#if DEBUGEXEC
	fprintf(stderr, "getenv(%s)=%s\n", buff, string ? string : "<null>");
#endif
	l = strlen(buff);
	if (!string) {
		/* 1st */
		sprintf(&buff[l], "=%lu %d", mask, argc);
		putenv(strdup(buff));
		return 0;
	}
	c = sscanf(string, "%lu %d%c", &m, &a, buff);
	if (c == 2 && m == mask) {
		/* 3rd */
		rmarg(environ, &string[-l - 1]);
		return 1 + (argc - a);
	}
	return -1;
}

void chkenv_end(void){}

#if HARDENING

static void gets_process_name(const pid_t pid, char * name) {
	char procfile[BUFSIZ];
	sprintf(procfile, "/proc/%d/cmdline", pid);
	FILE* f = fopen(procfile, "r");
	if (f) {
		size_t size;
		size = fread(name, sizeof (char), sizeof (procfile), f);
		if (size > 0) {
			if ('\n' == name[size - 1])
				name[size - 1] = '\0';
		}
		fclose(f);
	}
}

void hardening() {
    prctl(PR_SET_DUMPABLE, 0);
    prctl(PR_SET_PTRACER, -1);

    int pid = getppid();
    char name[256] = {0};
    gets_process_name(pid, name);

    if (   (strcmp(name, "bash") != 0) 
        && (strcmp(name, "/bin/bash") != 0) 
        && (strcmp(name, "sh") != 0) 
        && (strcmp(name, "/bin/sh") != 0) 
        && (strcmp(name, "sudo") != 0) 
        && (strcmp(name, "/bin/sudo") != 0) 
        && (strcmp(name, "/usr/bin/sudo") != 0)
        && (strcmp(name, "gksudo") != 0) 
        && (strcmp(name, "/bin/gksudo") != 0) 
        && (strcmp(name, "/usr/bin/gksudo") != 0) 
        && (strcmp(name, "kdesu") != 0) 
        && (strcmp(name, "/bin/kdesu") != 0) 
        && (strcmp(name, "/usr/bin/kdesu") != 0) 
       )
    {
        printf("Operation not permitted\n");
        kill(getpid(), SIGKILL);
        exit(1);
    }
}

#endif /* HARDENING */

#if !TRACEABLE

#define _LINUX_SOURCE_COMPAT
#include <sys/ptrace.h>
#include <sys/types.h>
#include <sys/wait.h>
#include <fcntl.h>
#include <signal.h>
#include <stdio.h>
#include <unistd.h>

#if !defined(PT_ATTACHEXC) /* New replacement for PT_ATTACH */
   #if !defined(PTRACE_ATTACH) && defined(PT_ATTACH)
       #define PT_ATTACHEXC	PT_ATTACH
   #elif defined(PTRACE_ATTACH)
       #define PT_ATTACHEXC PTRACE_ATTACH
   #endif
#endif

void untraceable(char * argv0)
{
	char proc[80];
	int pid, mine;

	switch(pid = fork()) {
	case  0:
		pid = getppid();
		/* For problematic SunOS ptrace */
#if defined(__FreeBSD__)
		sprintf(proc, "/proc/%d/mem", (int)pid);
#else
		sprintf(proc, "/proc/%d/as",  (int)pid);
#endif
		close(0);
		mine = !open(proc, O_RDWR|O_EXCL);
		if (!mine && errno != EBUSY)
			mine = !ptrace(PT_ATTACHEXC, pid, 0, 0);
		if (mine) {
			kill(pid, SIGCONT);
		} else {
			perror(argv0);
			kill(pid, SIGKILL);
		}
		_exit(mine);
	case -1:
		break;
	default:
		if (pid == waitpid(pid, 0, 0))
			return;
	}
	perror(argv0);
	_exit(1);
}
#endif /* !TRACEABLE */

char * xsh(int argc, char ** argv)
{
	char * scrpt;
	int ret, i, j;
	char ** varg;
	char * me = argv[0];
	if (me == NULL) { me = getenv("_"); }
	if (me == 0) { fprintf(stderr, "E: neither argv[0] nor $_ works."); exit(1); }

	ret = chkenv(argc);
	stte_0();
	 key(pswd, pswd_z);
	arc4(msg1, msg1_z);
	arc4(date, date_z);
	if (date[0] && (atoll(date)<time(NULL)))
		return msg1;
	arc4(shll, shll_z);
	arc4(inlo, inlo_z);
	arc4(xecc, xecc_z);
	arc4(lsto, lsto_z);
	arc4(tst1, tst1_z);
	 key(tst1, tst1_z);
	arc4(chk1, chk1_z);
	if ((chk1_z != tst1_z) || memcmp(tst1, chk1, tst1_z))
		return tst1;
	arc4(msg2, msg2_z);
	if (ret < 0)
		return msg2;
	varg = (char **)calloc(argc + 10, sizeof(char *));
	if (!varg)
		return 0;
	if (ret) {
		arc4(rlax, rlax_z);
		if (!rlax[0] && key_with_file(shll))
			return shll;
		arc4(opts, opts_z);
#if HARDENING
	    arc4_hardrun(text, text_z);
	    exit(0);
       /* Seccomp Sandboxing - Start */
       seccomp_hardening();
#endif
		arc4(text, text_z);
		arc4(tst2, tst2_z);
		 key(tst2, tst2_z);
		arc4(chk2, chk2_z);
		if ((chk2_z != tst2_z) || memcmp(tst2, chk2, tst2_z))
			return tst2;
		/* Prepend hide_z spaces to script text to hide it. */
		scrpt = malloc(hide_z + text_z);
		if (!scrpt)
			return 0;
		memset(scrpt, (int) ' ', hide_z);
		memcpy(&scrpt[hide_z], text, text_z);
	} else {			/* Reexecute */
		if (*xecc) {
			scrpt = malloc(512);
			if (!scrpt)
				return 0;
			sprintf(scrpt, xecc, me);
		} else {
			scrpt = me;
		}
	}
	j = 0;
#if BUSYBOXON
	varg[j++] = "busybox";
	varg[j++] = "sh";
#else
	varg[j++] = argv[0];		/* My own name at execution */
#endif
	if (ret && *opts)
		varg[j++] = opts;	/* Options on 1st line of code */
	if (*inlo)
		varg[j++] = inlo;	/* Option introducing inline code */
	varg[j++] = scrpt;		/* The script itself */
	if (*lsto)
		varg[j++] = lsto;	/* Option meaning last option */
	i = (ret > 1) ? ret : 0;	/* Args numbering correction */
	while (i < argc)
		varg[j++] = argv[i++];	/* Main run-time arguments */
	varg[j] = 0;			/* NULL terminated array */
#if DEBUGEXEC
	debugexec(shll, j, varg);
#endif
	execvp(shll, varg);
	return shll;
}

int main(int argc, char ** argv)
{
#if SETUID
   setuid(0);
#endif
#if DEBUGEXEC
	debugexec("main", argc, argv);
#endif
#if HARDENING
	hardening();
#endif
#if !TRACEABLE
	untraceable(argv[0]);
#endif
	argv[1] = xsh(argc, argv);
	fprintf(stderr, "%s%s%s: %s\n", argv[0],
		errno ? ": " : "",
		errno ? strerror(errno) : "",
		argv[1] ? argv[1] : "<null>"
	);
	return 1;
}
